```mermaid
erDiagram
	TENANT		||--|{ FUNCTIONID	: ont-to-many
	TENANT		||--|{ SERVICE	: ont-to-many
    FUNCTIONID  ||--|| RIGHT        : one-to-one
    SERVICE     ||--|{ FUNCTION     : ont-to-many
    FUNCTION    ||--|| RIGHT        : one-to-one
    ROLE        ||--|{ ROLE_RIGHT   : one-to-many
    ROLE_RIGHT  ||--|{ RIGHT        : one-to-many
    FUNCTIONID {
        string id
    } 
	SERVICE {
        string id
    }
    FUNCTION {
        string id
        string code
        string service_id
    }
    RIGHT {
        string id
        string module
        string category
        string menu
        string code
        string name
        string type
    }
    ROLE_RIGHT {
        string id
        string role_id
        string module
        string category
        string menu
        string code
        string type
    }
    ROLE {
        string id
    }
```

